#ifndef JSON_ENCODE_UNIT_TESTS_H
#define JSON_ENCODE_UNIT_TESTS_H

/**
 * Runs unit tests for JSON encoding.
 *
 * This function creates a JSON object representing an employee,
 * encodes it to a JSON string, and compares it with an expected JSON string.
 *
 * @return int
 *   EXIT_SUCCESS if the tests pass, EXIT_FAILURE otherwise.
 */
int run_json_encode_unit_tests();

#endif
